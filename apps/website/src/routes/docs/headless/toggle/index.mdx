---
title: Qwik UI | Toggle
---

import { statusByComponent } from '~/_state/component-statuses';

<StatusBanner status={statusByComponent.headless.Toggle} />

# Toggle

A two-state button that can be either on or off.

<Showcase name="hero" />

## ✨ Features

<FeatureList
  features={[
    'Follows the WAI-Aria design pattern',
    'Full keyboard navigation',
    'Can open one or multiple items at a time',
    'Supports initial and reactive values',
  ]}
/>

## Usage / Component State

### Initial Value (Uncontrolled)

An initial, uncontrolled value can be provided using the `defaultPressed` prop.

<Showcase name="defaultPressed" />

### Reactive Value (Controlled)

Pass the `pressed` prop to make the pressed state controlled.
The `onPressedChange$` event is fired when the user toggle the button, and receives the new value.

<Showcase name="pressed" />

Or you can bind the value with a signal.

<Showcase name="bind-pressed" />

### Disabled

Pass the `disabled` prop.

<Showcase name="disabled" />

## Building blocks

<CodeSnippet name="building-blocks" />

## Accessibility

### Keyboard interaction

<KeyboardInteractionTable
  keyDescriptors={[
    {
      keyTitle: 'Space',
      description: 'Toggles between states.',
    },
    {
      keyTitle: 'Enter',
      description: 'Toggles between states.',
    },
  ]}
/>

## API

<APITable
  propDescriptors={[
    {
      name: 'defaultPressed',
      type: 'boolean',
      description: 'An initial value when the toggle is rendered. Default to `false`.',
    },
    {
      name: 'pressed',
      type: 'boolean',
      description:
        'To make the pressed state controlled. Use in conjunction with `onPressedChange$`.',
    },
    {
      name: 'onPressedChange$',
      type: 'function',
      info: 'PropFunction<() => void>',
      description: 'Called when the state changes.',
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: 'Disables the toggle making the toggle unpressable.',
    },
  ]}
/>

import { component$ } from '@builder.io/qwik';
import { Badge } from '@qwik-ui/headless';

export default component$(() => {
  return <Badge style="padding: 4px; background-color: blue;">Badge</Badge>;
});

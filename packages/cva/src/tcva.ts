// import { cva } from './cva';
// import { stringifyClassList } from './stringify-classlist';

// export function tcva<Props>(classList: ClassList, config: Config<Props>) {
//   const variantsFn = cva(classList, config);
//   return function wrappedVariantsFn(props: Props): string {
//     return twMerge(stringifyClassList(variantsFn(props)));
//   };
// }
